# ON-CPU / OFF-CPU

需要 `bcc-tools`（`profile-bpfcc` / `offcputime-bpfcc`）以及 root 权限。

## ON-CPU

```bash
sudo profile-bpfcc -p <pid> -f 99 > oncpu.txt
```

## OFF-CPU

```bash
sudo offcputime-bpfcc -p <pid> > offcpu.txt
```
